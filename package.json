{
  "name": "math-fast",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "dev": "concurrently \"bun run dev:server\" \"bun run dev:client\"",
    "dev:server": "cd server && dotenv -e ../.env -- bun run dev",
    "dev:client": "cd client && dotenv -e ../.env -- bun run dev",
    "deploy": "cd client && bun run deploy"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
